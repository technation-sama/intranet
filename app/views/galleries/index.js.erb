$('#pins').append("<%= j render @galleries %>");

<% if @galleries.current_page == @galleries.total_pages %>
  $('#view-more').remove();
<% else %>
  $('#view-more a').attr('href', '<%= url_for(page: @galleries.current_page + 1) %>');
<% end %>