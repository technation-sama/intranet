<div class="col-md-4 col-lg-3 col-sm-6 box-gallery">
  <div class="gallery-index" id="gallery<%= gallery.id %>" style="background-image: url('<%= gallery.gallery_attachments.first.photo.url unless gallery.gallery_attachments.empty? %>');">
    <%= link_to gallery_gallery_attachments_path(gallery), class: "link-to-gallery-show" do %>          
      <div>
        <h4>
          <span>
            <span><%= gallery.gallery_attachments.count %></span>
            photos
          </span>
        </h4>
      </div>
      <h2><%= gallery.title %></h2>
    <% end %>
  </div>
</div>